# debug_app.py  (save this as app.py, overwriting the current file)

import streamlit as st

st.set_page_config(page_title="Debug", layout="wide")
st.title("🚀 Debug app – it works!")
st.write("If you see this text, the Streamlit container started correctly.")
